"""Placeholder test module — real tests will be added per-component."""


def test_placeholder() -> None:
    """Always passes; exists so pytest exits with code 0 at the scaffold stage."""
    assert True
